# Backend API base URL (the storyloom backend's /api/v1).
NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1

# Supabase (same project as the backend) — used for parent auth only.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
